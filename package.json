{
  "name": "text-string",
  "version": "1.0.0",
  "description": "Generate different text strings types for faster import of data in text objects.",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.47.0",
    "@types/intl": "^1.2.0",
    "intl": "^1.2.5",
    "typescript": "*"
  }
}
